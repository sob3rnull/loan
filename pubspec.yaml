name: money_loan_sync
description: Offline-first Android loan tracking app with manual .mloan sync.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  device_info_plus: ^11.2.2
  file_picker: ^8.1.6
  intl: ^0.20.2
  path: ^1.9.0
  path_provider: ^2.1.5
  provider: ^6.1.2
  share_plus: ^10.1.4
  sqflite: ^2.4.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
